"use client";

import { styled } from "@mui/material";

export const SectionPrivacy = styled('section')(({ theme }) => ({     
    width: '100%',    
    background: 'url("assets/images/background/BACKGROUND-DEFAULT.jpg") no-repeat center',
    backgroundSize: 'cover',
    height: '100%',
    display: 'flex',
    flexDirection: 'column',    
    '.opacity': {
        backgroundColor: 'rgba(0, 0, 0, 0.8)',
        width: '100%',
        height: '100%'
    }    
}));