import { styled } from "@mui/material";

export const HeaderContainer = styled("div")({
  textAlign: "center",
  position: "relative",
  
  "& .title": {
    fontSize: "3rem",
    fontWeight: "bold",
    color: "#0D95F9",
    marginBottom: "2rem",
  }
});